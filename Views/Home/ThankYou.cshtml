@{
    ViewData["Title"] = "Thank You";
}

<div class="container text-center py-5">
    <div class="py-5">
        <div class="display-1 text-success mb-4">
            <i class="bi bi-check-circle-fill"></i>
        </div>
        <h1 class="display-4 fw-bold">Thank You!</h1>
        <p class="lead mt-3 text-muted">Your message has been successfully received and safely stored in-memory.</p>
        <div class="mt-5">
            <a asp-action="Index" class="btn btn-outline-primary btn-lg rounded-pill px-5">Back to Home</a>
        </div>
    </div>
</div>
